"""Every ensemble prompt, as a Markdown file, plus the loader that fills its placeholders.

The `.md` files here are the prompts themselves — authored text, reviewed as prose rather
than as code. `library.py` holds the only Python in this package. Import it directly; this
module stays empty so reading a prompt never pulls in the service that sends it.
"""
