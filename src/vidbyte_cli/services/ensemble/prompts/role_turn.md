<task>
{{task}}
</task>

Investigate the workspace from your assigned perspective, then return your slate of
{{minimum}} to {{maximum}} approaches as the single JSON object described in your
instructions. Name every file an approach would change in its `files` array, so the agent that
compares approaches can see where yours and another member's touch the same place.
