"""Package marker for the `research` command namespace.

Deliberately empty of re-exports: `commands/__init__.py` imports each command class from its
own module, so adding a verb here never widens what a help path has to import.
"""
