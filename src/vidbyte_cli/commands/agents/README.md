# Agents commands

`agents suggest run` generates ideas, `categories` lists the taxonomy, and `handoff` extracts one packet. All three are local and free. Run accepts narrow context flags, including repeatable `--completed`, `--in-progress`, and `--future-intended-work` values. Future intended work describes planned but unfinished work, so a suggestion may refine or sequence it without claiming it is complete.
