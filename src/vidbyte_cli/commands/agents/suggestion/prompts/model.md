The generator model controls which configured model drafts the candidate pool. Explicit selection supports reproducibility and deliberate quality, latency, or cost tradeoffs.

An omitted selection delegates to the provider's established default. The chosen model still receives the same context contract and must satisfy the same structured output schema.

Changing the generator model does not change category definitions or final validation. The critic may use a distinct model when independent review needs a different tradeoff.
