The requested count is the maximum number of worthwhile ideas in the final slate. Five balances useful breadth with the attention required to compare candidates.

Values from one through twenty support narrow decisions and broader exploration. The generator considers a larger bounded pool before whole-slate refinement selects the strongest subset.

The count is never a quota that justifies weak filler. A smaller result includes an explicit shortfall so the caller can distinguish selectivity from failure.
