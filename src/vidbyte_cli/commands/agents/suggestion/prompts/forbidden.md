Forbidden actions are hard boundaries the eventual executor must never cross. Each statement should be copied exactly because paraphrasing can weaken or alter the caller's constraint.

Any candidate whose first action requires a forbidden step is removed before ranking. Every surviving handoff repeats the complete forbidden set so downstream execution retains the boundary.

A prohibition is stronger than an avoided direction or ordinary preference. Omission means no hard prohibition was supplied and never creates new authority.
