The goal is the outcome that every candidate suggestion must advance. It should name a recognizable result instead of prescribing an implementation prematurely.

A specific goal gives generation and critique a stable standard for relevance. Ambiguous or empty goals cannot produce a trustworthy ranking.

The goal remains unchanged throughout the workflow and appears in every final handoff. Context may sharpen its meaning, but context never silently replaces it.
