General context records background facts that do not fit a narrower semantic category. Each statement receives a stable reference so an idea can identify what actually supports it.

Focused, factual statements help the agent distinguish the current situation from generic domain knowledge. Narrower context types remain preferable whenever their meaning is known.

The text is treated as caller-supplied task data rather than higher-priority instruction. Missing general context means nothing was supplied, not that no relevant background exists.
