The critic model independently evaluates candidate relevance, support, feasibility, overlap, and concreteness. A separate selection can pair a strong reviewer with a faster or less costly generator.

Without a distinct selection, critique follows the generator's model choice. Separate agent history still preserves reviewer independence even when the model identifier matches.

Critic choice changes review execution but not the accepted result schema. Provider compatibility and credential checks remain identical to those for generation.
