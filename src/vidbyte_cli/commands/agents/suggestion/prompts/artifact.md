An artifact is an explicitly selected local text source offered as evidence. Its identity, digest, and stable context reference preserve the connection between a claim and its source.

Only readable supported text forms enter the bounded snapshot. Oversized content is truncated or omitted with a visible manifest status.

Artifacts provide evidence rather than instructions or authority. A missing or malformed source is rejected before the agent can produce unsupported suggestions from it.
