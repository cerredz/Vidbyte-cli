# Research domain

This folder owns strict research vocabulary and state policy. Persistent thread, run,
source, artifact, and cursor identifiers are opaque strings.

No provider API keys, Click objects, HTTP paths, or HTTPX types belong here. Request models
mirror the public behavior needed by the CLI while the final adapter owns route envelopes.
