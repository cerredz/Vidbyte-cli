# Research commands

This package owns the static `vidbyte-cli research` Click surface. Commands translate
validated CLI values into application inputs and delegate all execution, polling,
journaling, and route behavior. Registration must remain safe for help, version, and
credential-free startup paths.
