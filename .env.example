# Development overrides for the Vidbyte CLI. Production users configure via `vidbyte login`.

# Override the Vidbyte API host (defaults to https://api.vidbyte.ai).
VIDBYTE_API_URL=

# Vidbyte API key override for local development. Never commit a real key.
VIDBYTE_API_KEY=
