# Base URL of the Vidbyte public API. Defaults to https://api.vidbyte.ai when unset.
VIDBYTE_API_URL=https://api.vidbyte.ai

# Vidbyte API key. `vidbyte-cli login` persists this to ~/.vidbyte/credentials.json;
# setting it here overrides the stored key for the current shell.
VIDBYTE_API_KEY=
